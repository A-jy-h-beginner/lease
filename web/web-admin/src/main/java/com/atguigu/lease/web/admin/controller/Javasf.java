package com.atguigu.lease.web.admin.controller;

import java.util.*;

public class Javasf {
    public void main(int[] nums, int target) {
//        int i=0, j=0;
//        for(i = 0; i < nums.length - 1; i++){
//            for(j = i+1; j < nums.length; j++){
//                if(nums[i] + nums[j] == target){break;}
//            }
//            if(j != nums.length){break;}
//        }
//        int len = nums.length;
//        HashMap<Integer, Integer> map = new HashMap<>(len - 1);
//        // 存第一个
//        map.put(nums[0], 0);
//        for(int i = 1; i < len; i++){
//            int another = target - nums[i];
//            if(map.containsKey(another)){
//                return new int[]{i, map.get(another)};
//            }
//            map.put(nums[i], i);
//        }

//        String[] strs = null;
//        HashMap<String, List<String>> map = new HashMap<>();
//        for (String str : strs) {
//            char[] chars = str.toCharArray();
//            Arrays.sort(chars);
//            String s = new String(chars);
//            if(!map.containsKey(s)){
//                ArrayList<String> list = new ArrayList<>();
//                list.add(str);
//                map.put(s, list);
//            }else{
//                map.get(s).add(str);
//            }
//        }
//        return new ArrayList<>(map.values());

//        Arrays.sort(nums);
//        HashMap<Integer, Integer> map = new HashMap<>();
//        map.put(100, 0);
//        for (int i = 0; i < nums.length - 1; i++) {
//            int num = nums[i + 1] - nums[i];
//            // 判断是否差距为1
//            if (num != 1) {
//                if(map.get(100) < map.get(1)){
//                    map.put(100, map.get(1));
//                }
//                continue;
//            }
//            //
//            if (!map.containsKey(num)) {
//                map.put(num, 2);
//            } else {
//                map.put(num, map.get(num) + 1);
//            }
//        }
//        map.get(1).intValue() == null ? 0:map.get(1).intValue();
    }
}
